# Full PostgreSQL connection URL injected by runtime and compose.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP listen port.
PORT=8080

# Legacy fallback port used only when PORT is empty.
APP_PORT=8080
